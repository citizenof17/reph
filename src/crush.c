//
// Created by pavel on 07.05.2020.
//

#include "crush.h"

// n - number of elements to select
// t - type of elements to be selected
void crush_select(int n, bucket_class_e t){
    bucket_t * selected_buckets;


}

void * my_select(int n, bucket_class_e t){
    bucket_t * input;


    bucket_t * output;
    int i;
    for (i = 0; i < input->size; i++){
        int f = 0;

        int r;
        for (r = 0; r < n; r++){
            int fr = 0;

            char retry_descent = 0;

            do {
                bucket_t *bucket = &input[i];

            }
            while(!retry_descent);
        }
    }
}

